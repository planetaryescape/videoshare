{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "jsx": "react-jsx",
    "moduleResolution": "bundler",
    "types": ["bun"],
    "lib": ["ESNext", "DOM", "DOM.Iterable"]
  },
  "include": ["server.ts", "src/**/*.ts", "scripts/**/*.ts", "vite.config.ts", "vitest.config.ts"],
  "exclude": ["node_modules"]
}
