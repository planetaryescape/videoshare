{
  "name": "@videoshare/viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/player.entry.ts --outfile ./generated/player.js --target browser --format esm --minify && bun build ./src/player.css --outfile ./generated/player.css --target browser --minify"
  },
  "dependencies": {
    "@effect/sql-d1": "4.0.0-beta.78",
    "@videoshare/shared": "workspace:*",
    "effect": ">=4.0.0-beta.78 || >=4.0.0",
    "vidstack": "^0.6.15"
  }
}
