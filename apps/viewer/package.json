{
  "name": "@videoshare/viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test ./src",
    "build": "bun build ./src/player.entry.ts --outfile ./generated/player.js --target browser --format esm --minify && bun build ./src/player.css --outfile ./generated/player.css --target browser --minify && bun build ./src/project.entry.ts --outfile ./generated/project.js --target browser --format esm --minify && bun build ./src/project.css --outfile ./generated/project.css --target browser --minify"
  },
  "dependencies": {
    "@effect/sql-d1": "4.0.0-beta.101",
    "@videoshare/shared": "workspace:*",
    "effect": ">=4.0.0-beta.101 || >=4.0.0",
    "vidstack": "^0.6.15"
  }
}
